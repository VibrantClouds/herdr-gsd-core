---
phase: 01-component-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-component-decomposition/01-BASELINE.md
autonomous: true
requirements: [DECOMP-01, DECOMP-02, DECOMP-03]
must_haves:
  truths:
    - "Chrome/Chromium is confirmed available so `pnpm test` can run in this environment"
    - "The pre-extraction pass/fail state of the two existing target specs is recorded as an attributable baseline"
  artifacts:
    - ".planning/phases/01-component-decomposition/01-BASELINE.md"
  key_links:
    - "Baseline record → every later extraction plan attributes any spec regression against it"
---

<objective>
Establish the test-execution baseline BEFORE any extraction work begins, so any later spec failure can be correctly attributed to the refactor versus pre-existing state. This is the Wave 0 gate required by 01-VALIDATION.md and RESEARCH.md Open Question 1.

Two facts must be locked in first: (1) Chrome/Chromium is actually available for Karma (the research sandbox lacked it), and (2) the current pass/fail of `upload-modal.component.spec.ts` and `attachment-preview.component.spec.ts` — including the known pre-existing DI-mock mismatch in the upload-modal spec (it mocks the deprecated `UserModelService`, not the injected `IndexedDBUserModelService`, per D-06 note and RESEARCH.md Pitfall 3) — which must NOT be fixed in this phase.

Purpose: Give Waves 2-4 a trustworthy "before" snapshot so behavior-preservation (Success Criterion #5) is verifiable rather than assumed.
Output: `.planning/phases/01-component-decomposition/01-BASELINE.md` recording environment + per-spec baseline results and full-suite runtime.
</objective>

<execution_context>
@/home/user/Development/SizeComparisonSite/.claude/gsd-core/workflows/execute-plan.md
@/home/user/Development/SizeComparisonSite/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-component-decomposition/01-VALIDATION.md
@.planning/phases/01-component-decomposition/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm Chrome/Chromium availability and record full-suite runtime</name>
  <files>.planning/phases/01-component-decomposition/01-BASELINE.md</files>
  <read_first>
    - .planning/phases/01-component-decomposition/01-VALIDATION.md (Wave 0 Requirements + Test Infrastructure table)
    - .planning/phases/01-component-decomposition/01-RESEARCH.md (Environment Availability section — Chrome absence in research sandbox, correct `pnpm exec ng test` invocation form)
  </read_first>
  <action>
    Run the smoke check `pnpm exec ng test --include='**/app.component.spec.ts' --watch=false --browsers=ChromeHeadless` to confirm Karma can launch a headless browser. If it fails with "No binary for ChromeHeadless browser on your platform", set `CHROME_BIN` or run `npx puppeteer browsers install chrome` first, then re-run; do NOT proceed to any extraction plan until this passes. Then run the full suite once with `pnpm test --no-watch` and record the wall-clock runtime. Create `.planning/phases/01-component-decomposition/01-BASELINE.md` with an "Environment" section capturing: node version (`node --version`), pnpm version, whether ChromeHeadless launched, the exact command used, and the measured full-suite runtime. Do NOT modify any source or spec file in this task.
  </action>
  <verify>
    <automated>pnpm exec ng test --include='**/app.component.spec.ts' --watch=false --browsers=ChromeHeadless</automated>
  </verify>
  <acceptance_criteria>
    - The smoke-check command exits 0 (ChromeHeadless launched and app.component.spec ran).
    - `.planning/phases/01-component-decomposition/01-BASELINE.md` exists and contains an "Environment" section naming ChromeHeadless availability, the node/pnpm versions, and a recorded full-suite runtime value (not the literal placeholder text).
    - No file under `src/` was modified (git status shows only the BASELINE.md addition).
  </acceptance_criteria>
  <done>ChromeHeadless confirmed working; full-suite runtime measured and recorded in 01-BASELINE.md.</done>
</task>

<task type="auto">
  <name>Task 2: Record pre-extraction pass/fail baseline for the two existing target specs</name>
  <files>.planning/phases/01-component-decomposition/01-BASELINE.md</files>
  <read_first>
    - src/app/components/upload-modal/upload-modal.component.spec.ts (note line 4 imports `UserModelService`; lines 25-42 provide/inject it; line 82-95 "should validate unique name")
    - src/app/components/attachment-preview/attachment-preview.component.spec.ts (tests call `component.renderPreview`, `component.previewScale`, `component.setupResizeObserver` — these define the surface later plans must preserve)
    - .planning/phases/01-component-decomposition/01-RESEARCH.md (Pitfall 3 — the UserModelService vs IndexedDBUserModelService DI-mock mismatch)
  </read_first>
  <action>
    Run each target spec in isolation and record the exact pass/fail counts:
    `pnpm exec ng test --include='**/upload-modal.component.spec.ts' --watch=false --browsers=ChromeHeadless` and
    `pnpm exec ng test --include='**/attachment-preview.component.spec.ts' --watch=false --browsers=ChromeHeadless`.
    Append a "Spec Baseline" section to 01-BASELINE.md recording, per spec file: total specs, passed, failed, and the name of any failing `it(...)` block. Explicitly note whether "should validate unique name" (upload-modal spec) currently passes or fails, and record the pre-existing DI-mock mismatch (spec mocks deprecated `UserModelService`; component injects `IndexedDBUserModelService`) as KNOWN/PRE-EXISTING per D-06 — do NOT change the spec's mock in this phase (fixing it is out of scope; TEST work is Phase 5). This record is the attribution baseline: any later plan whose extraction turns a currently-green test red must reconcile against this file.
  </action>
  <verify>
    <automated>pnpm exec ng test --include='**/attachment-preview.component.spec.ts' --watch=false --browsers=ChromeHeadless</automated>
  </verify>
  <acceptance_criteria>
    - 01-BASELINE.md contains a "Spec Baseline" section with pass/fail counts for BOTH `upload-modal.component.spec.ts` and `attachment-preview.component.spec.ts`.
    - The record explicitly states the current pass/fail status of the "should validate unique name" test and labels the `UserModelService`/`IndexedDBUserModelService` mock mismatch as pre-existing and out-of-scope for this phase.
    - No `*.spec.ts` file was modified (git status shows only BASELINE.md changed under version control).
  </acceptance_criteria>
  <done>Both existing target specs' pass/fail counts recorded in 01-BASELINE.md with the known upload-modal mock mismatch flagged as pre-existing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced by this plan) | This plan runs tests and writes a planning-doc record only; it introduces no new code path or trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | npm/pnpm installs | high | accept | No new packages introduced this phase (pure internal refactor; RESEARCH.md forbids adding `@angular/cdk`). If Chrome must be installed via `npx puppeteer browsers install chrome`, that is a dev-only test dependency, not an app dependency. |

Later plans that relocate coordinate/listener logic inherit and mitigate T-01-01..03 (defined in 01-02-PLAN.md).
</threat_model>

<verification>
- ChromeHeadless launches and runs specs (`pnpm exec ng test ... --browsers=ChromeHeadless` exits 0).
- Full suite runs to completion at least once (`pnpm test --no-watch`).
- 01-BASELINE.md exists with Environment + Spec Baseline sections.
</verification>

<success_criteria>
- Chrome/Chromium availability confirmed; no extraction plan proceeds until this is true.
- Attributable pre-extraction baseline recorded for the two existing target specs.
- Zero source/spec files modified by this plan.
</success_criteria>

## Artifacts this phase produces (this plan)

- New file: `.planning/phases/01-component-decomposition/01-BASELINE.md` (Environment + Spec Baseline record)

<output>
Create `.planning/phases/01-component-decomposition/01-01-SUMMARY.md` when done, and ensure `01-BASELINE.md` is committed.
</output>
