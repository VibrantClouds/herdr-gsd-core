---
phase: 01-component-decomposition
plan: 09
type: execute
wave: 5
depends_on: ["01-07", "01-08"]
files_modified:
  - .planning/phases/01-component-decomposition/01-BASELINE.md
autonomous: true
requirements: [DECOMP-01, DECOMP-02, DECOMP-03]
must_haves:
  truths:
    - "No file under upload-modal/, attachment-edit-modal/, or attachment-preview/ exceeds ~400 lines (ROADMAP Success Criterion #1)"
    - "The full test suite is green, with any deviation from the 01-BASELINE.md pre-extraction baseline explicitly reconciled and explained"
    - "The three manual editType walkthroughs recorded in 01-08-SUMMARY.md all passed"
  artifacts:
    - ".planning/phases/01-component-decomposition/01-BASELINE.md (updated with post-extraction reconciliation section)"
  key_links:
    - "01-BASELINE.md pre-extraction record → post-extraction full-suite result (attribution)"
---

<objective>
Close out Phase 1 (Component Decomposition) with the phase-wide gate required by ROADMAP.md Success Criterion #5 ("Existing specs for these three components still pass and manual upload/edit/preview workflows behave identically — `pnpm test` green, no behavior change") and 01-VALIDATION.md's Phase Gate requirement (full suite green PLUS manual attachment-edit-modal walkthrough — already completed in Plan 08).

This plan runs the full suite ONE final time across all merged Wave 2-4 changes, audits line counts on all three target directories against the ~400-line guideline (DECOMP-01/02/03), and reconciles any spec outcome differences against the `01-BASELINE.md` pre-extraction record from Plan 01 — so any deviation is either explained as pre-existing (per Pitfall 3's known DI-mock mismatch) or flagged as a genuine regression to fix before the phase is considered complete.

Purpose: Provide a single, auditable close-out record proving DECOMP-01/02/03 and the phase's behavior-preservation requirement are met.
Output: `01-BASELINE.md` updated with a "Post-Extraction Reconciliation" section; no source changes expected (fix-forward only if this gate finds a genuine regression).
</objective>

<execution_context>
@/home/user/Development/SizeComparisonSite/.claude/gsd-core/workflows/execute-plan.md
@/home/user/Development/SizeComparisonSite/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-component-decomposition/01-BASELINE.md
@.planning/phases/01-component-decomposition/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full suite, audit line counts, reconcile against baseline</name>
  <files>.planning/phases/01-component-decomposition/01-BASELINE.md</files>
  <read_first>
    - .planning/phases/01-component-decomposition/01-BASELINE.md (Plan 01's pre-extraction Environment + Spec Baseline record — the attribution source)
    - .planning/phases/01-component-decomposition/01-05-SUMMARY.md, 01-07-SUMMARY.md, 01-08-SUMMARY.md (final line counts and per-spec pass outcomes recorded by the wave 3/4 plans, if present)
    - .planning/ROADMAP.md (Phase 1 Success Criteria — the five items this gate must confirm)
  </read_first>
  <action>
    Run `pnpm test --no-watch` for the full suite. Run `wc -l src/app/components/upload-modal/*.ts src/app/components/attachment-edit-modal/*.ts src/app/components/attachment-preview/*.ts` (and any newly-created sub-component/service `.ts` files under `src/app/components/measurement-ruler/`, `src/app/components/attachment-point-picker/`, `src/app/components/angle-dial/`, `src/app/services/measurement-ruler.service.ts`, `src/app/services/attachment-point-definition.service.ts`, `src/app/services/attachment-canvas-renderer.service.ts`, `src/app/services/upload-image-pipeline.service.ts`, `src/app/services/upload-form-validators.service.ts`) to confirm every file is under ~400 lines. Append a "Post-Extraction Reconciliation" section to `01-BASELINE.md` containing: (1) the full-suite pass/fail summary (total specs, passed, failed, and names of any failing tests), (2) a line-by-line comparison against the pre-extraction baseline recorded in the same file's "Spec Baseline" section — for `upload-modal.component.spec.ts` and `attachment-preview.component.spec.ts`, explicitly state whether the pass/fail outcome MATCHES the pre-extraction baseline (expected) or DIFFERS (flag as a regression requiring investigation, unless it is the already-documented pre-existing "should validate unique name" DI-mock mismatch), (3) the full list of file line counts confirming the ~400-line target, and (4) a confirmation that Plan 08's three manual editType walkthroughs all passed (cross-reference `01-08-SUMMARY.md`). If ANY new failure is found that is not attributable to the documented pre-existing mismatch, do NOT mark this task done — instead note the specific failing test and file for follow-up (this phase's scope does not include fixing failures unrelated to the decomposition itself, but a regression CAUSED by the decomposition must be fixed by editing the relevant file from its owning plan before this gate can close).
  </action>
  <verify>
    <automated>pnpm test --no-watch</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test --no-watch` completes (pass or documented-failure outcome recorded).
    - Every file under `upload-modal/`, `attachment-edit-modal/`, and `attachment-preview/` is confirmed under ~400 lines in the reconciliation record (or any exception is explicitly justified, e.g. `.scss`/`.html` files are not subject to the ~400-line TS guideline).
    - `01-BASELINE.md` contains a "Post-Extraction Reconciliation" section explicitly comparing post-extraction spec outcomes against the pre-extraction baseline, with any difference either explained as pre-existing or flagged as a regression.
    - The reconciliation record confirms all three manual `editType` walkthroughs from `01-08-SUMMARY.md` passed.
  </acceptance_criteria>
  <done>Full suite result and line-count audit are recorded and reconciled against the pre-extraction baseline; the phase's behavior-preservation requirement (ROADMAP Success Criterion #5) is either confirmed satisfied or any genuine regression is explicitly flagged for fix-forward before phase close.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan is a verification/audit gate; it introduces no new code path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | npm/pnpm installs | high | accept | No packages installed this plan. |

This gate confirms the mitigations recorded in 01-02/01-03/01-04/01-06/01-07/01-08's threat registers (T-01-01 through T-01-04) held after full integration — no new threats introduced by this audit-only plan.
</threat_model>

<verification>
- Full suite runs to completion; outcome recorded and reconciled against `01-BASELINE.md`.
- All target component/service/sub-component files confirmed under the ~400-line guideline.
- All three manual editType walkthroughs confirmed passed.
</verification>

<success_criteria>
- ROADMAP.md Phase 1 Success Criteria 1-5 are all confirmed true, or any gap is explicitly flagged with an owning follow-up.
- `01-BASELINE.md` is the definitive, auditable record of behavior-preservation for this phase.
</success_criteria>

## Artifacts this phase produces (this plan)

- Modified `.planning/phases/01-component-decomposition/01-BASELINE.md` — adds "Post-Extraction Reconciliation" section

<output>
Create `.planning/phases/01-component-decomposition/01-09-SUMMARY.md` when done, stating the final pass/fail state of the phase gate.
</output>
