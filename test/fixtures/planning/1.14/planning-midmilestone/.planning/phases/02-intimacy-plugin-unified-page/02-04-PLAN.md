---
phase: 02-intimacy-plugin-unified-page
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - pnpm-lock.yaml
autonomous: false
requirements: [CHAR-05]

estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A human has confirmed @angular/cdk is the official Angular Component Dev Kit (npm publisher angular, repository github.com/angular/components) before it is installed"
    - "apps/web depends on @angular/cdk at the same 22.1.x minor as @angular/core, and @angular/cdk/drag-drop and @angular/cdk/a11y resolve from apps/web"
    - "No other dependency is added or bumped by the install"
  artifacts:
    - path: "apps/web/package.json"
      provides: "@angular/cdk dependency"
      contains: "\"@angular/cdk\""
  key_links:
    - from: "apps/web/package.json"
      to: "pnpm-lock.yaml"
      via: "pnpm --filter web add resolves one @angular/cdk version"
      pattern: "@angular/cdk@22\\.1\\."
---

<objective>
Install `@angular/cdk` for the drag-and-drop reorder (CHAR-05), the reorder live announcements, and the add-page dialog focus trap, behind the package-legitimacy gate.

Purpose: plans 02-05 and 02-07 import `@angular/cdk/drag-drop` and `@angular/cdk/a11y`. The legitimacy check returned SUS (reason "too-new": the 22.1.6 patch was published 2026-09-09) even though the package is the official Angular CDK with ~2.9M weekly downloads. RESEARCH.md recorded an override, but a SUS verdict requires a human confirmation that no automated run may approve.
Output: `@angular/cdk` in `apps/web/package.json` and `pnpm-lock.yaml`.
</objective>

<execution_context>
@~/.claude-getshitdone/gsd-core/workflows/execute-plan.md
@~/.claude-getshitdone/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-intimacy-plugin-unified-page/02-RESEARCH.md
@docs/adr/0016-angular-signals-no-ui-framework.md

Rules for every executor in this phase:
- Stage files by explicit path. Never run `git add -A` or `git add .`.
- Do not co-author commits as Claude (user CLAUDE.md).

<interfaces>
- `apps/web/package.json` pins `@angular/core` `^22.1.0`; `pnpm-lock.yaml` resolves `@angular/core@22.1.6`.
- Legitimacy signals (run 2026-09-14, `gsd-tools query package-legitimacy check --ecosystem npm @angular/cdk`): exists, not deprecated, no postinstall, repo `git+https://github.com/angular/components.git`, weekly downloads 2,865,998, verdict SUS, reasons ["too-new"].
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm @angular/cdk is the legitimate Angular Component Dev Kit before install</name>
  <what-built>Nothing is installed yet. The automated legitimacy check flagged @angular/cdk as SUS only because its newest patch (22.1.6) is five days old. ADR-0016 names it as the app's single sanctioned UI dependency.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@angular/cdk and confirm the publisher is the Angular team (maintainers include "angular"), the repository link is github.com/angular/components, and the weekly downloads are in the millions.
    2. Confirm version 22.1.6 is listed and is published from the same release train as @angular/core 22.1.6 (https://www.npmjs.com/package/@angular/core?activeTab=versions).
    3. Confirm the package has no install scripts listed on the npm page.
  </how-to-verify>
  <resume-signal>Type "approved" to install @angular/cdk, or describe what looked wrong.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add @angular/cdk to apps/web at the core-matched version</name>
  <precondition>Task 1 checkpoint was answered "approved" by a human in this run</precondition>
  <files>apps/web/package.json, pnpm-lock.yaml</files>
  <read_first>
    - apps/web/package.json
    - pnpm-workspace.yaml (allowBuilds entries; the CDK has no build script, so none should be needed)
  </read_first>
  <action>
Run `pnpm --filter web add @angular/cdk@^22.1.0` from the repo root (ADR-0016: drag-drop and a11y only; no @angular/material). The resolved version must share the 22.1 minor with the locked @angular/core. If pnpm reports ERR_PNPM_IGNORED_BUILDS for anything, stop and report instead of editing allowBuilds. Do not change any other dependency range. Record the resolved version in the SUMMARY.
  </action>
  <verify>
    <automated>node -e "const p=require('./apps/web/package.json');if(!/^\^22\.1\./.test(p.dependencies['@angular/cdk']||''))process.exit(1)" && rg -q "'@angular/cdk@22\.1\." pnpm-lock.yaml && (cd apps/web && node --input-type=module -e "await import('@angular/cdk/drag-drop'); await import('@angular/cdk/a11y');")</automated>
    <fails_when>non-zero exit, or "Cannot find package" / "ERR_MODULE_NOT_FOUND" in stderr</fails_when>
    <automated>pnpm --filter "web..." run build</automated>
    <fails_when>non-zero exit from the production build</fails_when>
  </verify>
  <acceptance_criteria>
    - `apps/web/package.json` lists `@angular/cdk` with a `^22.1.` range under dependencies.
    - `rg -c '@angular/material' apps/web/package.json` prints nothing and exits 1.
    - `git show HEAD -- apps/web/package.json` for this task's commit shows exactly one added line (`@angular/cdk`) and no removed lines.
    - The production build passes.
  </acceptance_criteria>
  <done>@angular/cdk is installed at the core-matched 22.1 version after a human confirmed its legitimacy, and its drag-drop and a11y entry points resolve.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry to workspace | Third-party code enters the build and the shipped bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SC | Tampering | pnpm install of @angular/cdk | high | mitigate | Package-legitimacy gate: blocking-human checkpoint (Task 1) on the SUS verdict before install; version pinned to the core-matched 22.1 minor; lockfile committed. |
| T-02-04-01 | Elevation of Privilege | install-time scripts | medium | mitigate | CDK has no postinstall (legitimacy signals); pnpm allowBuilds is left unchanged, so any unexpected build script fails the install instead of running. |
</threat_model>

<verification>
- `pnpm --filter "web..." run build` exits 0.
- `pnpm --filter web exec ng test --watch=false` exits 0 (no regressions from the dependency).
</verification>

<success_criteria>
- @angular/cdk is available to plans 02-05 (drag-drop, LiveAnnouncer) and 02-07 (cdkTrapFocus), with a recorded human legitimacy confirmation.
</success_criteria>

## Artifacts this phase produces

- `apps/web/package.json`: dependency `@angular/cdk` (`^22.1.x`)
- `pnpm-lock.yaml`: resolved `@angular/cdk@22.1.x`

<output>
Create `.planning/phases/02-intimacy-plugin-unified-page/02-04-SUMMARY.md` when done
</output>
