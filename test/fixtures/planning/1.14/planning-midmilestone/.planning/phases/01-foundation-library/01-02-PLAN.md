---
phase: 01-foundation-library
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/specs/SPEC-design-system.md
  - docs/specs/SPEC-gallery-and-portrait.md
  - docs/specs/SPEC-frontend-architecture.md
autonomous: true
requirements: [CHAR-01, SCHM-02]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "SPEC-design-system.md section 4.1 Masthead describes the portrait as the full image at its natural aspect ratio (object-fit: contain) inside a bounded frame, never cropped to a circle or a square, with a non-circular monogram placeholder when portrait is null and exact sizing decided in Phase 4 (D-P1)"
    - "SPEC-gallery-and-portrait.md Portrait 'Header rendering' bullet states the same presentation rule and names Phase 4 (GALL-04) as the owner of exact frame sizing (D-P1)"
    - "SPEC-frontend-architecture.md section 1 directory sketch places plugin fixtures inside each plugin directory, envelope fixtures under packages/schema/src/fixtures/envelope/, and the guard at packages/schema/src/__tests__/fixture-guard.spec.ts, with no top-level test/ directory under packages/schema (D-P2)"
    - "Exactly the three edited spec files are staged and committed; every other file under docs/ stays untracked"
  artifacts:
    - path: "docs/specs/SPEC-design-system.md"
      provides: "Masthead portrait presentation rule per D-P1"
      contains: "object-fit: contain"
    - path: "docs/specs/SPEC-gallery-and-portrait.md"
      provides: "Portrait header rendering rule per D-P1"
      contains: "non-circular monogram"
    - path: "docs/specs/SPEC-frontend-architecture.md"
      provides: "Monorepo directory sketch matching the per-plugin fixture layout per D-P2"
      contains: "__tests__/"
  key_links:
    - from: "docs/specs/SPEC-frontend-architecture.md"
      to: "docs/specs/SPEC-serialization-policy.md"
      via: "Both name the same guard path and fixture layout"
      pattern: "fixture-guard\\.spec\\.ts"
---

<objective>
Correct three locked specs so they agree with the user's planning-run decisions before any code is written against them. This is a documentation-only plan. The phase tracer lives in 01-01.

- D-P1: the character header shows the full, uncropped character art at its natural aspect ratio. Two specs still describe a circular or square crop and must say otherwise.
- D-P2: fixtures live per plugin (`packages/schema/src/plugins/<type>/fixtures/<version>.json`), envelope fixtures live under `packages/schema/src/fixtures/envelope/`, and the guard is `packages/schema/src/__tests__/fixture-guard.spec.ts`. The directory sketch in SPEC-frontend-architecture.md still shows a top-level `test/` directory.

Purpose: the user's rule is that when a document already covers a topic, it gets updated in place. Plans 01-06 (character header) and 01-03 (fixture guard) read these specs, so they must be correct first.
Output: three edited spec files, committed by explicit path.
</objective>

<execution_context>
@~/.claude-getshitdone/gsd-core/workflows/execute-plan.md
@~/.claude-getshitdone/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/specs/SPEC-design-system.md
@docs/specs/SPEC-gallery-and-portrait.md
@docs/specs/SPEC-frontend-architecture.md
@docs/specs/SPEC-serialization-policy.md

Repo facts the executor must respect:
- `docs/` is untracked in git but is the real source, not a mirror. Stage ONLY the three files this plan edits, by explicit path. Never run a bulk `git add` (no `-A`, no `.`, no `docs/`); that would start tracking the whole untracked `docs/` tree.
- Never co-author commits as Claude.
- User decision D-P1 (verbatim intent): "People are going to use character art of all kind, the full image probably should be used somehow." The header shows the full image, uncropped, at its natural aspect ratio. No circle, and no square cover-fit crop. When there is no portrait, show a non-circular monogram. Exact sizing is Phase 4's job.
- User decision D-P2: per-plugin fixture layout (paths above).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the portrait presentation rule in SPEC-design-system.md and SPEC-gallery-and-portrait.md (D-P1)</name>
  <files>docs/specs/SPEC-design-system.md, docs/specs/SPEC-gallery-and-portrait.md</files>
  <read_first>
    - docs/specs/SPEC-design-system.md (section 4.1 Masthead, the Anatomy line at line 126; also section 1.1 and 1.3 for the token names --surface-2, --border, --radius-lg)
    - docs/specs/SPEC-gallery-and-portrait.md (Portrait section, the "Header rendering" bullet at line 15)
  </read_first>
  <action>
Per D-P1, edit two lines in place. Do not add a new document and do not add a changelog note about the change.

1. In docs/specs/SPEC-design-system.md, section 4.1 Masthead, the line starting "Anatomy:" currently describes the portrait as a fixed-size circular crop at two breakpoints with an initials fill. Replace only the parenthesised portrait clause. The replacement text is: portrait (the full image at its natural aspect ratio, `object-fit: contain`, inside a bounded frame; never cropped to a circle or a square; when `portrait` is null, a non-circular monogram placeholder (first letter of the name) on `--surface-2` in the same frame; exact frame sizing is decided in Phase 4). Keep the rest of that Anatomy line character-for-character: the `.eyebrow` clause, the `.title-input` name clause, and the `.meta-row` clause.

2. In docs/specs/SPEC-gallery-and-portrait.md, Portrait section, replace the whole "Header rendering:" bullet (currently a square-crop presentation with fixed mobile and desktop pixel sizes and an initial-letter monogram) with this text: Header rendering: the full image at its natural aspect ratio (`object-fit: contain`) within a bounded frame, never cropped to a circle or a square, because character art comes in every shape and is shown whole; a non-circular monogram placeholder (first letter of the name) when null. Exact frame sizing is decided in Phase 4 (GALL-04). Leave the other three Portrait bullets unchanged.

Stage and commit only these two files by explicit path.
  </action>
  <verify>
    <automated>rg -q 'object-fit: contain' docs/specs/SPEC-design-system.md && rg -q 'non-circular monogram' docs/specs/SPEC-design-system.md && rg -q 'object-fit: contain' docs/specs/SPEC-gallery-and-portrait.md && rg -q 'non-circular monogram' docs/specs/SPEC-gallery-and-portrait.md && ! rg -q '96 px circle' docs/specs/SPEC-design-system.md && ! rg -q 'object-fit: cover' docs/specs/SPEC-gallery-and-portrait.md && ! rg -q 'aspect-ratio: 1' docs/specs/SPEC-gallery-and-portrait.md && rg -q 'title-input' docs/specs/SPEC-design-system.md && echo OK</automated>
    <fails_when>non-zero exit or "OK" is not printed: a required phrase is missing, or a removed crop phrase is still present</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -c 'object-fit: contain' docs/specs/SPEC-design-system.md` prints a count of at least 1.
    - `rg -c 'object-fit: contain' docs/specs/SPEC-gallery-and-portrait.md` prints a count of at least 1.
    - Both files contain the phrase `non-circular monogram`.
    - `rg -q '96 px circle' docs/specs/SPEC-design-system.md` exits 1 (phrase gone).
    - `rg -q 'object-fit: cover' docs/specs/SPEC-gallery-and-portrait.md` and `rg -q 'aspect-ratio: 1' docs/specs/SPEC-gallery-and-portrait.md` both exit 1.
    - The section 4.1 Anatomy line in SPEC-design-system.md still contains `.eyebrow`, `.title-input` and `.meta-row`.
    - Both edited lines mention Phase 4 as the owner of exact frame sizing.
    - `git show --stat HEAD` lists exactly these two paths for the task commit.
  </acceptance_criteria>
  <done>Both specs state the full-image, natural-aspect, non-cropped portrait rule with a non-circular monogram for the null case, and nothing else in either file changed.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the packages/schema directory sketch in SPEC-frontend-architecture.md (D-P2)</name>
  <files>docs/specs/SPEC-frontend-architecture.md</files>
  <read_first>
    - docs/specs/SPEC-frontend-architecture.md (section 1 Monorepo layout code block, lines 24-40, the packages/schema subtree)
    - docs/specs/SPEC-serialization-policy.md (sections "What a bump requires" and "Fixture-coverage guard", the authoritative paths)
  </read_first>
  <action>
Per D-P2, edit the section 1 code block in docs/specs/SPEC-frontend-architecture.md so the `packages/schema` subtree matches the per-plugin fixture layout. Make exactly these changes:

1. Add `fixtures/<version>.json` to the brace list of each plugin directory that has one. The intimacy entry becomes `intimacy/{v1.ts,schema.ts,migrations.ts,index.ts,fixtures/<version>.json}`, and the gallery entry changes the same way. Leave the `bio/...` and `physical/...` Phase 8 lines as they are.
2. Directly under `src/`, as siblings of `plugins/` and at the same indentation, add a `fixtures/` directory entry. Give it one child line, `envelope/<version>.json`, with the aligned comment `# one per supported envelope version`.
3. Also directly under `src/`, add an `__tests__/` directory entry. Give it one child line, `fixture-guard.spec.ts`, with the aligned comment `# fixture set equals supportedVersions, for the envelope and every plugin`.
4. Delete the three-line `test/` subtree that sits directly under `packages/schema/`: the directory line and both of its child lines.

Match the existing two-space indentation steps. Keep `#` comments aligned with the neighbouring comment column. Change nothing else in the file.

Stage and commit only this file by explicit path.
  </action>
  <verify>
    <automated>F=docs/specs/SPEC-frontend-architecture.md; ! rg -q 'fixtures/<type>/<version>.json' "$F" && ! rg -q '^\s+test/\s*$' "$F" && rg -q '__tests__/' "$F" && rg -q 'envelope/<version>.json' "$F" && rg -q 'intimacy/\{v1.ts,schema.ts,migrations.ts,index.ts,fixtures/<version>.json\}' "$F" && [ "$(rg -c 'fixture-guard.spec.ts' "$F")" = "1" ] && echo OK</automated>
    <fails_when>non-zero exit or "OK" is not printed: the old top-level test/ subtree or old fixture path remains, or a required new entry is missing or duplicated</fails_when>
  </verify>
  <acceptance_criteria>
    - `rg -q 'fixtures/<type>/<version>.json' docs/specs/SPEC-frontend-architecture.md` exits 1.
    - `rg -q '^\s+test/\s*$' docs/specs/SPEC-frontend-architecture.md` exits 1.
    - `rg -c 'fixture-guard.spec.ts' docs/specs/SPEC-frontend-architecture.md` prints `1`, and that line sits under an `__tests__/` entry.
    - The sketch contains `envelope/<version>.json` under a `fixtures/` entry that is a sibling of `plugins/`.
    - The intimacy and gallery brace lists both end with `fixtures/<version>.json}`.
    - `git ls-files docs` lists exactly `docs/specs/SPEC-design-system.md`, `docs/specs/SPEC-frontend-architecture.md` and `docs/specs/SPEC-gallery-and-portrait.md`.
  </acceptance_criteria>
  <done>The SPEC-frontend-architecture directory sketch agrees with SPEC-serialization-policy and SPEC-subdocument-plugin-contract on fixture and guard locations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Documentation-only change. No runtime code, input handling or deployment surface is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02-01 | Tampering | docs/specs edits | low | accept | The change is prose in three spec files. It is reviewed by commit diff and has no runtime effect. |
| T-01-02-02 | Information Disclosure | git staging of the untracked docs/ tree | low | mitigate | Stage by explicit path only. The acceptance check `git ls-files docs` must list exactly the three edited files. |
</threat_model>

<verification>
- `git ls-files docs` prints exactly the three edited spec paths.
- Both task `<automated>` checks print OK.
</verification>

<success_criteria>
- D-P1 is reflected in both portrait specs: full image, natural aspect ratio, `object-fit: contain`, no circle and no square crop, non-circular monogram when null, Phase 4 owns sizing.
- D-P2 is reflected in the SPEC-frontend-architecture sketch.
- No other documentation changed or was committed.
</success_criteria>

## Artifacts this phase produces

This plan creates no code symbols. It modifies:
- `docs/specs/SPEC-design-system.md`: section 4.1 Masthead portrait clause.
- `docs/specs/SPEC-gallery-and-portrait.md`: Portrait "Header rendering" bullet.
- `docs/specs/SPEC-frontend-architecture.md`: section 1 `packages/schema` directory sketch (`fixtures/envelope/<version>.json`, `__tests__/fixture-guard.spec.ts`, per-plugin `fixtures/<version>.json`).

<output>
Create `.planning/phases/01-foundation-library/01-02-SUMMARY.md` when done
</output>
